假设我们做一个分类任务：

输入：西瓜重量  
输出：是否成熟

我们用一个规则：

> 如果重量 > 1.5kg → 成熟

现在我们问：

如果我用 100 个样本训练，  
这个规则在未来数据上还能正确吗？

PAC 理论说：

如果样本数足够大，  
那么“以高概率”模型的真实误差会很小。

数学表达：

对于任意 ε（允许误差）  
对于任意 δ（失败概率）

存在样本数 n，使得：

Pr(真实误差 ≤ ε) ≥ 1 − δ

这就是 PAC。

一句话总结：

PAC 是“泛化保证”的数学框架。